name: collection_picker
description: A Package contains selectable listview and gridview widgets with support 
  for single, multi, and radio picker types. The data is generic so it can be used using any data. 
  Help you to create a filters, multi choices and etc.
version: 0.0.1
repository: https://github.com/nandakista/collection_picker

environment:
  sdk: '>=2.18.4 <3.0.0'

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  lints: ^2.0.0
  test: ^1.21.0
